[package]
name = "scylla-model"
description = "Scylla domain model — the durable RE narrow waist (DD-001)."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
